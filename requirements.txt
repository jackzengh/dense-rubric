# NOTE: no vllm / no deepspeed on purpose — single-GPU sequential generate+train.
# torch is listed unpinned: Colab ships a torch already matched to its CUDA driver,
# so `pip install -r requirements.txt` will keep it; locally it installs the latest.
torch
transformers>=4.57.0
datasets>=3.0.0
google-genai>=1.55.0
pydantic>=2.0.0
pandas
pyarrow
numpy
wandb
bitsandbytes; sys_platform == "linux"
python-dotenv
ipywidgets
